// import { updateUserStreak } from '../../app/api/streak';
// import { supabase } from '../../lib/supabase';

// describe('Streak API', () => {
//   it('should update user streak based on activity', async () => {
//     const userId = 1;
//     const result = await updateUserStreak(userId);
//     expect(result.streak).toBeGreaterThan(0);
//   });
// });
